# SPDX-License-Identifier: Apache-2.0

menuconfig AM2301B
	bool "Aosong AM2301B Sensor"
	depends on I2C
	help
	  Enable driver for Aosong AM2301B sensor.

if AM2301B

config AM2301B_CRC_CHECK
	bool "Enable CRC Check"
	default y
	help
	  Check the crc value after data reading.

endif # AM2301B
